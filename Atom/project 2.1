//import Scanner
import java.util.Scanner;

//start program
public class Main{
  static Scanner scan = new Scanner(System.in);
  public static void main(String[] args){
//intitiate variables
String operation = "";
int total = 0;
//welcome message
System.out.println("Welcome to the Kaleb Calculator 2.1");
//imput # 1
total = getInputOne();
// input number 2
// do the operation with the 2 numbers
do{
  int numberTwo = getInputTwo();
total = getOperationandDoMath(total, numberTwo);
System.out.println(total);
}while(!operation.equals("e") && !operation.equals("exit"));
  }

  public static int getInputOne(){
    System.out.println("Input Number One");
    while(!scan.hasNextInt()){
      String garbage = scan.nextLine();
      System.out.println(garbage + " is not a number");
      System.out.println("Input Number One");
    }
    int total = scan.nextInt();
    return total;
  }
  public static int getInputTwo(){
    System.out.println("Input Number Two");
    while(!scan.hasNextInt()){
      String garbage = scan.next();
      System.out.println(garbage + " is not a number");
      System.out.println("Input Number Two");
    }
    int numberTwo = scan.nextInt();
    return numberTwo;
  }
  public static int getOperationandDoMath(int total, int numberTwo){
    //list all mathmatical operations
    String operation = "";
   System.out.println("Choose a operation from the following");
    System.out.print("ADD, +, SUBTRACT, -, MULTIPLY, *, DIVIDE, /, MODLUS, %,");
    System.out.println("EXIT, E");
    operation = scan.next();
    operation = operation.toLowerCase();
    // do the operation with the 2 numbers
    switch(operation){
      case "add":
      case "+":
      total = addition(total, numberTwo);
      break;

      case "subtract":
      case "-":
      total = subtraction(total, numberTwo);
        break;

      case "multiply":
      case "*":
      total = multiplication(total, numberTwo);
        break;

      case "divide":
      case "/":
      total = division(total, numberTwo);
           break;

      case "modulus":
      case "%":
      total = modulus(total, numberTwo);
      break;

      case "e":
       case "exit":
       System.out.println("Your final total was " + total);
    System.out.print(" Thank you for using the Kaleb Calculator 2.1");
    scan.close();
    System.exit(0);
    }
    return total;
  }
  public static int addition(int total, int numberTwo){
    total = total + numberTwo;
    System.out.println("Your Total is: " + total);
    return total;
  }
   public static int subtraction(int total, int numberTwo){
    total = total - numberTwo;
    System.out.println("Your Total is: " + total);
    return total;
  }
   public static int multiplication(int total, int numberTwo){
    total = total * numberTwo;
    System.out.println("Your Total is: " + total);
    return total;
  }
   public static int division(int total, int numberTwo){
    total = total / numberTwo;
    System.out.println("Your Total is: " + total);
    return total;
  }
   public static int modulus(int total, int numberTwo){
    total = total % numberTwo;
    System.out.println("Your Total is: " + total);
    return total;
  }
}
